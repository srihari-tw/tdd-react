import Header from "../components/Header";

const StoreLocator = () => {
  return (
    <div data-testid="storeLocator">
      Store Locator
      <Header />
    </div>
  );
};

export default StoreLocator;
